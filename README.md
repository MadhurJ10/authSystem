Authentication System

This project implements a basic authentication system with features like user registration, login, logout, and password reset.
Built using Node.js, Express, and MongoDB.

Note: The server is hosted on Render, so it might take a few seconds to wake up if it’s been inactive.
